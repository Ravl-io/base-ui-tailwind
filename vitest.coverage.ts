// Imports all source modules so v8 can instrument them for coverage reporting.
// Without this, only files imported by tests appear in the coverage report.
import "./src/components";
import "./src/hooks";
import "./src/lib";
